---
layout: post
---

github_for_windows入れた
よく調べずに適当にコマンド打ってる状態だけど

git add *
リポジトリにファイル追加
ローカルのディレクトリにファイル置いただけじゃ追加されないからgit addコマンドが必要

git pull
別の場所で行われた変更を同期（更新）

git commit * -m "comment"
コミット

git push
ローカルのをリモートへ

とりあえずこんだけ覚えた

gh-pagesが更新されない罠にさっそく嵌ってた
今回の原因はconfigか何かのファイルの記述ミスがあって
jekyllによるメタが行われなくなってた思われる
エラー表示出る場所ないからヒストリって大事

